# ══════════════════════════════════════════════════════════════════════════════
# Deepfake & Image Manipulation Detection — Requirements
# Project 4  |  Parts 1, 2, 3 + Bonus (Eye-Blink Temporal Analysis)
# ══════════════════════════════════════════════════════════════════════════════
# Install:  pip install -r requirements.txt
# Python:   3.9 – 3.12 recommended  (MediaPipe does NOT support Python 3.13)
# ──────────────────────────────────────────────────────────────────────────────

# ── Deep Learning ─────────────────────────────────────────────────────────────
tensorflow>=2.12.0,<2.17.0        # CNN deepfake classifier (MobileNetV2)
                                   # TF 2.17+ requires separate keras package

# ── Computer Vision ───────────────────────────────────────────────────────────
opencv-python>=4.8.0               # Face detection, ELA, image processing
                                   # Also used as fallback for blink analysis
Pillow>=10.0.0                     # Image loading and format conversion

# ── Scientific Computing ──────────────────────────────────────────────────────
numpy>=1.24.0                      # Array operations throughout
scipy>=1.10.0                      # DCT frequency analysis (ai_content_detector)
scikit-learn>=1.3.0                # Train/test split, metrics
scikit-image>=0.21.0               # Edge detection, image filters (splicing_detector)

# ── Data & Visualisation ──────────────────────────────────────────────────────
matplotlib>=3.7.0                  # Training history plots, heatmap visualisations
tqdm>=4.65.0                       # Progress bars (dataset_loader)

# ── Report Generation ─────────────────────────────────────────────────────────
jinja2>=3.0.0                      # HTML report templating
fpdf2>=2.7.0                       # PDF report generation

# ── Explainability ────────────────────────────────────────────────────────────
shap>=0.42.0                       # SHAP explainability (optional — may conflict
                                   # with newer TF; simple_explainer.py is the
                                   # active fallback and has no SHAP dependency)

# ── Reverse Image Search ──────────────────────────────────────────────────────
imagehash>=4.3.1                   # Perceptual hashing (reverse_image_search.py)

# ── Bonus: Eye-Blink Temporal Analysis ────────────────────────────────────────
# MediaPipe is OPTIONAL — blink_analyzer.py falls back to OpenCV automatically.
# Install for higher landmark accuracy (Python 3.9–3.12 ONLY):
#
#   pip install mediapipe
#
# MediaPipe ≤ 0.9  uses mp.solutions.face_mesh  (legacy API)
# MediaPipe ≥ 0.10 uses mp.tasks.vision.FaceLandmarker (Task API, needs model download)
# Python 3.13      — MediaPipe broken (ctypes issue); OpenCV fallback used automatically
#
# mediapipe>=0.10.0                # Uncomment if on Python 3.9–3.12
